import React from 'react';

import { Box } from '@/components/Layout';
import { type CommonTypes } from '@/types/common';

export interface {{properCase name}}Props extends CommonProps {}

export const {{properCase name}} = (props: {{properCase name}}Props) => {
  return <Box {...props}>{{properCase name}}</Box>;
};
