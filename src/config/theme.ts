import { createTheme } from '@rneui/themed';

export const theme = createTheme({
  lightColors: {
    greys: {
      0: '#0d0e13',
      50: '#111218',
    },
  },
  darkColors: {
    greys: {
      0: '#0d0e13',
      50: '#111218',
    },
  },
  mode: 'light',
});
